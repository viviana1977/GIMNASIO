import tkinter as tk
from tkinter import ttk
from tkinter import *

import gimnasio_modelo as gc

ventana_principal = tk.Tk()
ventana_principal.title("GIMNASIO")
ventana_principal.geometry("800x600")
ventana_principal.resizable(0,0)
principal = ttk.Notebook(ventana_principal)
principal.pack()
principal_frame = ttk.Frame(principal)
principal.add(principal_frame, text="GIMNASIO")
notebook = ttk.Notebook(principal_frame)
notebook.pack(pady=10, padx=10, fill='both', expand=True)
ventana_principal.mainloop()

